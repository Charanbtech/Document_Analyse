# Document Classification System — Environment Config
# Copy to .env and fill in your values

# Optional: MongoDB Atlas connection string
# Leave blank to use SQLite (default)
MONGO_URI=

# Flask settings
FLASK_ENV=development
FLASK_DEBUG=1
PORT=5000
